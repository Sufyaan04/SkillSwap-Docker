services:
  backend:
    build: ./backend
    container_name: skillswap-backend
    ports:
      - "5000:5000"
    env_file:
      - ./backend/.env
    restart: unless-stopped

  frontend:
    build: ./frontend
    container_name: skillswap-frontend
    ports:
      - "80:80"
    depends_on:
      - backend
    restart: unless-stopped